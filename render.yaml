services:
  - type: web
    name: trading-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python trading_bot_telegram.py"
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
